import GemLoader from "@/components/GemLoader";

export default function Loading() {
  return (
    <div className="page-loading page-loading--dark">
      <GemLoader variant="dark" />
    </div>
  );
}
